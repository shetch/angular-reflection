export class Meal {
  idCategory: string;
  strCategory: string;
  strCategoryThumb: string;

  /* deserialize(input: any) {
    return Object.assign(this, { id: input.idCategory,
                                        category: input.strCategory,
                                        thumb: input.strCategoryThumb,
                                        description: input.strCategoryDescription
    })
  } */
}
